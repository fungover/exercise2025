spring:
  main:
    web-application-type: none
  application:
    name: exercise2025

  threads:
    virtual:
      enabled: true

#  datasource:
#    url: jdbc:mysql://localhost:3306/exercise2025?useSSL=false&serverTimezone=UTC
#    username: ${MYSQL_USER}
#    password: ${MYSQL_PASSWORD}