package org.example.factory;

public class UnknownAnimal implements Animal{
    //Null Object Design Pattern
    //Behavioral pattern, used when we don't want to return null
    // but instead return a default object
}
